apiVersion: extensions/v1
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "role-ingress"
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /role
            backend:
              serviceName: role-service
              servicePort: 80
