apiVersion: extensions/v1
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "project-assessment-ingress"
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /project
            backend:
              serviceName: project-assessment-site
              servicePort: 80
