apiVersion: extensions/v1
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "office-ingress"
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /office
            backend:
              serviceName: office-service
              servicePort: 80
