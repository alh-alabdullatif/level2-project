apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: "office-ingress"
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /office
            backend:
              serviceName: office-service
              servicePort: 80
