apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: "department-ingress"
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /department
            backend:
              serviceName: department-service
              servicePort: 80
